// missed_classes маршрут
const MISSED_CLASSES_ROUTE = {
  PATH: "/missed_classes",
  NAME: "missed_classes",
};

export { MISSED_CLASSES_ROUTE };

const missed_classes_routes = [
  {
    path: MISSED_CLASSES_ROUTE.PATH,
    name: MISSED_CLASSES_ROUTE.NAME,
  },
];

export default missed_classes_routes;
