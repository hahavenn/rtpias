/* subjects маршрут */

const SUBJECT_ROUTE = {
  PATH: "/subjects",
  NAME: "subjects",
};

const subjects_routes = [
  {
    path: SUBJECT_ROUTE.PATH,
    name: SUBJECT_ROUTE.NAME,
  },
];

export default subjects_routes;
